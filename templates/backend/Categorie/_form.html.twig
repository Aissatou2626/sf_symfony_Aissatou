    <form action="{{ path('admin.categories.delete', {id:categorie.id}) }}" method="post" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette catégorie ?')">
        <input type="hidden" name="token" value="{{ csrf_token('delete' ~ categorie.id) }}">
        <button type="submit">Supprimer</button>
    </form>